<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ page isErrorPage="true" %>
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Ошибка</title>
    <link rel="stylesheet" href="styles/style.css">
</head>
<body>
    <div id="header">
        <h1>Произошла ошибка</h1>
        <p>Николенко Максим Викторович, группа P3215, вариант 466890</p>
    </div>

    <div class="container">
        <div class="error-message">
            <%
                Integer statusCode = (Integer) request.getAttribute("javax.servlet.error.status_code");
                String errorMessage = (String) request.getAttribute("javax.servlet.error.message");
                String requestUri = (String) request.getAttribute("javax.servlet.error.request_uri");

                if (statusCode != null) {
                    out.println("<h3>Код ошибки: " + statusCode + "</h3>");
                }
                if (errorMessage != null && !errorMessage.isEmpty()) {
                    out.println("<p><strong>Сообщение:</strong> " + errorMessage + "</p>");
                }
                if (requestUri != null) {
                    out.println("<p><strong>Запрошенный URI:</strong> " + requestUri + "</p>");
                }
                if (exception != null) {
                    out.println("<p><strong>Исключение:</strong> " + exception.getMessage() + "</p>");
                }
            %>
        </div>

        <div style="text-align: center; margin-top: 30px;">
            <a href="controller" class="submit-btn" style="text-decoration: none; display: inline-block;">
                Вернуться на главную
            </a>
        </div>
    </div>
</body>
</html>