+ Еще раз проверить на все ошибки ввода файла
Проверка различной записи файла, добавить еще 4 функции, и проверить все базовые исключения. То есть все
Убрать данные об автомобиле( сразу true or false)\
+ Сделать адекватное сохранение файлов в правильном порядке
+ Удостоверить что чтение происходит через байтовый поток(InputStreamReader) и чтение через BuffedReader и всю основу которая нужна
* Создание нового продукта:
Введите имя персонажа:
... 523
Введите координату X:
... 5325
Введите координату Y:
... 5325
> Error: Поля продукта не валидны! Продукт не создан!

set HUMAN_DATA_FILE="C:\path\to\file.csv"
java -jar target/your-program.jar

Собрать jar архив
Проверить Runner
reduce by soundrackname
Atomic(атомарные операции) буферизация блокировка ввод-вывод

4931
max_by_real_hero
sum_of_impact_speed
print_field_descending_impact_speed
remove_by_sountrackname
filter_by_minutes_of_waiting minutesOfWaiting
print_ascending

add_if_min {element} : добавить новый элемент в коллекцию, если его значение меньше, чем у наименьшего элемента этой коллекции
remove_greater {element} : удалить из коллекции все элементы, превышающие заданный
min_by_name : вывести любой объект из коллекции, значение поля name которого является минимальным
filter_by_minutes_of_waiting minutesOfWaiting : вывести элементы, значение поля minutesOfWaiting которых равно заданному
print_ascending : вывести элементы коллекции в порядке возрастания
count_less_than_impact_speed impactSpeed : вывести количество элементов, значение поля impactSpeed которых меньше заданного
print_field_descending_impact_speed : вывести значения поля impactSpeed всех элементов в порядке убывания

public boolean removeById(int id) {
        return collection.removeIf(h -> h.getId() == id);
    }

    public boolean containsId(int id) {
        return collection.stream().anyMatch(h -> h.getId() == id);

        скрипт 1 - добавляет в коллекцию несколько элементов
        скрипт 2 - показывает инфо о коллекции и выдает список элементов
        скрипт 3 - вызывает несколько раз скрипт 1
        скрипт 4 - вызывает скрипт 3 и потом скрипт 2
    }

Почему методы в Creators private?